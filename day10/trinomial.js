// 삼항연산자
// x ? y : z        x가 참이면 y를 반환, x가 거짓이면 z를 반환


let score = 90;
let grade = score >= 90 ? 'A+' : 'A';
// score 값이 90을 넘거나 같으면 A+, 90점 아래면 A를 grade값으로 정하자
console.log(grade); // A+ : score값이 90점을 넘어서 grade값을 A+로 반환


let math = 80;
let avg = 90;
let english = 100;

let isMathLowAvg1 = math < avg;
console.log('isMathLowAvg1',isMathLowAvg);

let ismathLowAvg = english < avg;
console.log('isEnglishLowAvg1',isEnglishLowAvg);

let isMathLowAvg = math < avg;
console.log('isMathLowAvg1',isMathLowAvg);

// 강사님 코드 못따라감... 복사하기!!
